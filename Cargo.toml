[package]
name = "rust_json_parser"
version = "0.1.0"
edition = "2021"
authors = ["Tu Nombre <tuemail@example.com>"]
description = "Un parser JSON minimalista escrito en Rust"
license = "MIT"
readme = "README.md"

# --- Dependencias ---
[dependencies]
# serde = { version = "1.0", features = ["derive"] }
# serde_json = "1.0"

